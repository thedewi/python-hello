[project]
name = "python-hello"
version = "0.1.0"
description = "A command-line tool example"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyfiglet>=1.0.4",
]

[project.scripts]
python-hello = "main:main"
